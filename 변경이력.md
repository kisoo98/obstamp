# 타임스탬프 핫키 — 변경 이력

OBS 녹화/방송 중 핫키를 누르면 현재 시간을 자동 기록하는 프로그램의 개발 기록입니다.

---

## v0.4 — 방송/녹화 분리 기록 (최신)

방송과 녹화를 각각 별도의 타임라인 파일로 분리하고, 동시 진행 시 두 파일에 모두 기록하도록 개선했습니다.

> **배경:** 이전 버전에서는 방송+녹화를 동시에 켤 때, 먼저 켠 출력 기준으로 파일이 하나만 생성되었습니다(예: 방송을 먼저 켜면 녹화 시간이 빠진 `STREAM_..._timeline.txt` 하나만 생성). 방송과 녹화의 시작/종료가 명확히 분리되지 않던 문제를 해결한 것이 이번 변경입니다.

### 적용된 동작

**1. 방송 시작 → `STREAM_*_timeline.txt` 생성**

방송(스트리밍)을 시작하면 방송 전용 파일이 만들어집니다. 이 파일에는 **방송 시간**과 **실제 시각** 두 가지만 기록됩니다.

```
# 방송 타임라인
# 기록 시작  : 2026-06-07 14:00:00
# ────────────────────────────────────────────
# [번호]  방송 시간    실제 시각
# ────────────────────────────────────────────
[001]  00:03:22.1    14:03:34
[002]  00:07:45.8    14:07:58
```

**2. 녹화 시작 → `*_timeline.txt` (REC) 생성**

녹화를 시작하면 녹화 전용 파일이 만들어집니다. 이 파일에는 **방송 시간**, **녹화 시간**, **실제 시각** 세 가지가 기록됩니다.

```
# 녹화 파일  : 2026-06-07 14-00-00.mkv
# 기록 시작  : 2026-06-07 14:00:00
# ──────────────────────────────────────────────────────
# [번호]  방송 시간    녹화 시간    실제 시각
# ──────────────────────────────────────────────────────
[001]  00:03:22.1    00:01:10.4    14:03:34
[002]  00:07:45.8    00:05:33.9    14:07:58
```

> 방송 없이 녹화만 하는 경우, 방송 시간 칸은 `--:--:--.--` 로 표시됩니다.

**3. 녹화 + 방송 동시 진행**

두 상태가 모두 활성화되면 `STREAM_*_timeline.txt` 와 REC `*_timeline.txt` 가 **둘 다 생성**됩니다.

**4. 핫키 한 번 → 양쪽 파일에 동시 기록**

녹화+방송 상태에서 핫키(기본 `F8`)를 누르면, 두 파일에 **각자의 형식에 맞게** 타임스탬프가 동시에 기록됩니다.

- `STREAM` 파일에는 → 방송 시간 · 실제 시각
- `REC` 파일에는 → 방송 시간 · 녹화 시간 · 실제 시각

### 파일 생성/종료 규칙

| 상황 | 생성되는 파일 |
|------|---------------|
| 방송만 시작 | `STREAM_*_timeline.txt` |
| 녹화만 시작 | `*_timeline.txt` (REC, 방송 시간 칸은 빈 값) |
| 방송 + 녹화 동시 | 두 파일 모두 |

- 각 출력이 **종료될 때** 해당 파일이 자동으로 마무리(종료 시각·총 기록 개수 기록)됩니다.
- 녹화 종료 시, 타임라인 파일 이름이 OBS의 실제 녹화 파일명으로 자동 변경됩니다.
- 방송 파일은 방송이 종료될 때까지 유지됩니다(녹화를 먼저 끝내도 유지).

### 주요 구현 (timestamp_hotkey.py)

- `StreamTimeline` 클래스 — 방송 전용 파일(2개 열) 생성·기록·마무리
- `RecTimeline` 클래스 — 녹화 전용 파일(3개 열) 생성·기록·마무리
- `timeline` 단일 객체를 `stream_tl` / `rec_tl` 두 개로 분리
- `_on_stream_start` / `_on_stream_stop` — 방송 진입/종료 시 STREAM 파일 처리
- `_on_rec_start` / `_on_rec_stop` — 녹화 진입/종료 시 REC 파일 처리
- `_stamp` — 핫키 입력 시 활성화된 모든 타임라인에 각각 기록
- OBS WebSocket 이벤트 + 500ms 폴링 이중 감지로 상태 변화 누락 방지

---

## v0.3 — 방송 시간 기록 · 관리자 권한 · 안정화

이 단계에서 방송(스트리밍) 시간 기록 기능이 처음 추가되고, 핫키 동작 신뢰성과 UI 문제들이 정리되었습니다.

### 기능 추가

- **방송 시간 기록** — 핫키를 누를 때 녹화 시간뿐 아니라 방송 시간도 함께 타임라인에 기록.
- **방송 전용 활성화** — 녹화 중이 아니어도 방송 중이면 핫키 입력 가능. (성능 부담은 거의 없음 — 기존 0.5초 폴링에 스트리밍 상태 조회 API 호출 한 번만 추가)
- UI에 방송 시간을 녹화 시간 아래 작게(보라색) 표시.

### 버그 수정

- **설정 초기화 문제 해결** — PyInstaller로 만든 exe 실행 시 `__file__`이 임시 폴더를 가리켜, `config.json`(핫키·저장 위치·OBS 비밀번호)이 임시 폴더에 저장됐다가 종료 시 삭제되던 문제. exe 실행 시 `sys.executable` 기준(exe 옆 폴더)으로 설정을 저장·불러오도록 `_BASE_DIR`을 수정. `_on_rec_start`에 남아있던 `Path(__file__).parent` 잔재도 함께 정리.
- **핫키가 다른 창에서 안 먹는 문제** — 원인은 권한 차이(OBS 등 포커스된 앱이 관리자 권한이면 일반 권한 프로세스의 글로벌 핫키 후킹이 차단됨). `빌드.bat`에 `--uac-admin`을 추가해 exe가 관리자 권한을 요청하도록 변경.

### UI 수정

- **창 크기가 들쭉날쭉한 문제** — 상태 변경마다 레이블 텍스트 길이가 달라져 창이 늘었다 줄었다 하던 현상. 레이블에 고정 `width`를 지정하고, "OBS 연결 설정" 버튼을 `pack/pack_forget` 대신 자리를 항상 차지한 채 색만 바꿔 표시/숨김 처리.
- **상태 표시줄이 잘리는 문제** — `frm_status`의 `pack_propagate(False)`가 프레임 높이를 0으로 찌그러뜨려 상태 표시줄이 안 보이던 문제. 해당 한 줄 제거.
- **"대기 중" 글자가 안 보이는 문제** — 어두운 배경에 묻히던 `DARKGRAY(#444444)`를 `GRAY(#888888)`로 변경.

---

## v0.2

- 초기 GUI 버전 (tkinter 기반 우측 하단 위젯)
- OBS WebSocket 연결, 녹화 자동 감지, 단일 타임라인 파일 저장
- ⚙ 설정: 핫키 · 저장 위치 · OBS 비밀번호 변경

---

_최종 업데이트: 2026-06-07_
